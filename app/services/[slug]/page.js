import {notFound} from 'next/navigation';import Link from 'next/link';import {Header,Footer} from '../../components';import {services} from '../../data';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export default function Service({params}){const s=services.find(x=>x.slug===params.slug);if(!s)notFound();return <><Header/><main className="inner"><div className="wrap"><p className="eyebrow"><i/> Услуга</p><h1>{s.title}</h1><p className="lede">{s.text} Начинаем с бизнес-задачи, фиксируем ожидаемый результат и собираем решение, которое можно развивать.</p><div className="detail-grid"><div><span>01</span><h2>Разберёмся</h2><p>Изучим процессы, аудиторию, ограничения и точки роста.</p></div><div><span>02</span><h2>Спроектируем</h2><p>Подготовим структуру, сценарии, архитектуру и прозрачную оценку.</p></div><div><span>03</span><h2>Запустим</h2><p>Разработаем, протестируем, внедрим аналитику и останемся на поддержке.</p></div></div><Link className="button" href="/#contact">Обсудить задачу ↗</Link></div></main><Footer/></>}
